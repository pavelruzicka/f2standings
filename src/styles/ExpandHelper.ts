import styled from "styled-components"

import { RowBlock } from "./Row"

export const ExpandHelper = styled(RowBlock)`
  justify-content: center !important;
  color: #848484;
  font-size: 0.75rem;
  text-transform: uppercase;
`
